<h2>Latest Patterns</h2>
<ul id="pattern-list">
  {{#each pattern in content}}
    <li>{{#link-to 'pattern' pattern}}{{pattern-display pattern=pattern width=125 height=25}} {{!pattern.name}}{{/link-to}}</li>
  {{/each}}
  <li>{{#link-to 'patterns.new'}}Add a new pattern{{/link-to}}</li>
</ul>

{{outlet}}
